# Multi Tag Delete plugin for Inkdrop

Inkdrop multi tag delete plugin.

Allow selection of multiple tags within inkdrop, then pressing "Delete" will delete the tags - includes removing the tags from notes using them.

Also allow mass deletion of tags not used by any notes
